import * as React from 'react';
import SiteTemplate from 'templates/SiteTemplate';


const Home: React.FC = () => (
    <SiteTemplate>
        <h1>Home</h1>
    </SiteTemplate>
);

export default Home;